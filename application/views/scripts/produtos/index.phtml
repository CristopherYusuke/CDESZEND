<div class="row ">
    <div class="small-12 columns">
        <h2 class="text-center" > Listar Produto </h2>

        <form method="<?php echo $this->form->getMethod(); ?>" action="<?php echo $this->form->getAction(); ?>" class="custom">
            <div class="row ">
                <?php
                echo $this->form->descricao;
                echo $this->form->status;
                ?>
            </div>
            <div class="row ">
                <?php
                echo $this->form->submit;
                ?>
            </div>

        </form>
    </div>
</div>
<div class="row">
    <div class="small-12 small-centered  columns">
        <div class="scroll">
            <table width="100%">
                <thead>
                    <tr>
                        <th>#</th>
                        <th>Descricao</th>
                        <th>Preco Custo</th>
                        <th>Preco Venda</th>
                        <th>Estoque</th>
                        <th>Status</th>
                        <th>Editar</th>
                    </tr>
                </thead>
                <?php foreach ($this->produtos as $key => $value) {
                    ?>
                    <tr>
                        <td><?php echo $this->produtos[$key]['idProduto']; ?></td>
                        <td><?php echo $this->produtos[$key]['descricao']; ?></td>
                        <td><?php echo "R$" . number_format($this->produtos[$key]['precoCusto'], 2, ',', ''); ?></td>
                        <td><?php echo "R$" . number_format($this->produtos[$key]['precoVenda'], 2, ',', ''); ?></td>
                        <td><?php echo $this->produtos[$key]['estoque']; ?></td>
                        <?php if ($this->produtos[$key]['status']) : ?>
                            <td><span class="success label">Ativo</span> </td>   
                        <?php else: ?>
                            <td><span class="alert label">Inativo</span> </td> 
                        <?php endif; ?> 
                        <td width="50px"><a href="/produtos/update/idProduto/<?php echo $this->produtos[$key]['idProduto']; ?>" class="tiny button secondary">Editar</a></td>
                    </tr>
                <?php } ?>
            </table>
        </div>
    </div>
</div>
<div class="row">
    <div class="small-12  columns">
        <a id="btn_Cadastrar_usuario" href="/produtos/create" class="button">Novo</a>
    </div>
</div>


